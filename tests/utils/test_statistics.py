from helpers.utils import *
from helpers.imports import *


# TODO time_series_uniform(*, t_zero, max_days=1, size=100)

# TODO time_series_lognormal(*, t_zero, mean=0, sigma=1, size=100)

# TODO plot_time_series(series, resample_frequency="60T")

# TODO time_series_hist_diffs(series, units='d')

# TODO estimate_kernel_cdf(kernel, N=1000, verbose=False)

# TODO series_kde_gaussian_kernel(series, mss)

# TODO evaluate_pdf(kernel, series)

# TODO estimate_kernel_mean(kernel, x=None)

# TODO estimate_kernel_variance(kernel)

# TODO get_x_from_pcdf(kernel, queried_cdf, verbose=False)

