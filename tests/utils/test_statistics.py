from skepsi.utils.utils import *
from skepsi.utils.imports import *


# TODO
# time_series_uniform(*, t_zero, max_days=1, size=100)

# time_series_lognormal(*, t_zero, mean=0, sigma=1, size=100)

# plot_time_series(series, resample_frequency="60T")

# time_series_hist_diffs(series, units='d')

# estimate_kernel_cdf(kernel, N=1000, verbose=False)

# series_kde_gaussian_kernel(series, mss)

# evaluate_pdf(kernel, series)

# estimate_kernel_mean(kernel, x=None)

# estimate_kernel_variance(kernel)

# get_x_from_pcdf(kernel, queried_cdf, verbose=False)

